#!/bin/bash

filename="$HOME/Desktop/crop_$(date +"%Y-%m-%d_%T").png"
if [ "$XDG_SESSION_TYPE" = x11 ]; then
    exec import $filename
elif [ "$XDG_SESSION_TYPE" = wayland ]; then
    exec grim -g "$(slurp)" $filename
fi
