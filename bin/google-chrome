#!/bin/bash

set -- "--proxy-pac-url=file://$HOME/.imo.proxy.pac" "$@"

[[ -x /usr/bin/chromium ]] && exec /usr/bin/chromium "$@"
[[ -x /usr/bin/google-chrome ]] && exec /usr/bin/google-chrome "$@"

echo "You don't have google-chrome OR chromium" >&2
exit 1
